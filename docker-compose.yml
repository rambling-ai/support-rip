version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8007:8007"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}

  frontend:
    build: ./frontend
    ports:
      - "8081:80"
    depends_on:
      - backend
